package provider

// Note:  This file is automatically generated.  Manually made changes
// will be overwritten when the provider is generated.

import (
	"context"
	"strings"

	"github.com/paloaltonetworks/terraform-provider-prismasdwan/sdk"
	xNwmFxK "github.com/paloaltonetworks/terraform-provider-prismasdwan/sdk/sdwan/services/sdwan/v20/api/bulkconfigurations/sitetemplates/sitetemplateid/deployments"

	"github.com/hashicorp/terraform-plugin-framework/datasource"
	dsschema "github.com/hashicorp/terraform-plugin-framework/datasource/schema"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"
)

// Data source.
var (
	_ datasource.DataSource              = &bulkConfigPushDataSource{}
	_ datasource.DataSourceWithConfigure = &bulkConfigPushDataSource{}
)

func NewBulkConfigPushDataSource() datasource.DataSource {
	return &bulkConfigPushDataSource{}
}

type bulkConfigPushDataSource struct {
	client *sdwan.Client
}

// bulkConfigPushDsModel is the model.
type bulkConfigPushDsModel struct {
	Tfid types.String `tfsdk:"tfid"`

	// Input.
	DeploymentId   types.String `tfsdk:"deployment_id"`
	SiteTemplateId types.String `tfsdk:"site_template_id"`

	// Output.
	Etag        types.Int64  `tfsdk:"etag"`
	Id          types.String `tfsdk:"id"`
	VariableMap types.List   `tfsdk:"variable_map"`
}

// Metadata returns the data source type name.
func (d *bulkConfigPushDataSource) Metadata(_ context.Context, req datasource.MetadataRequest, resp *datasource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_bulk_config_push"
}

// Schema defines the schema for this data source.
func (d *bulkConfigPushDataSource) Schema(_ context.Context, _ datasource.SchemaRequest, resp *datasource.SchemaResponse) {
	resp.Schema = dsschema.Schema{
		Description: "Retrieves a config item.",

		Attributes: map[string]dsschema.Attribute{
			// inputs:map[string]bool{"deployment_id":true, "site_template_id":true} outputs:map[string]bool{"etag":true, "id":true, "tfid":true, "variable_map":true} forceNew:map[string]bool{"deployment_id":true, "site_template_id":true}
			"deployment_id": dsschema.StringAttribute{
				Description: "Deployment Id.",
				Required:    true,
			},
			"etag": dsschema.Int64Attribute{
				Description: "Entity tag for the resource.",
				Computed:    true,
			},
			"id": dsschema.StringAttribute{
				Description: "Id.",
				Computed:    true,
			},
			"site_template_id": dsschema.StringAttribute{
				Description: "Site Template Id.",
				Required:    true,
			},
			"tfid": dsschema.StringAttribute{
				Description: "The Terraform ID.",
				Computed:    true,
			},
			"variable_map": dsschema.ListAttribute{
				Description: "Variable Map.",
				Computed:    true,
				ElementType: types.BoolType,
			},
		},
	}
}

// Configure prepares the struct.
func (d *bulkConfigPushDataSource) Configure(_ context.Context, req datasource.ConfigureRequest, resp *datasource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}

	d.client = req.ProviderData.(*sdwan.Client)
}

// Read performs Read for the struct.
func (d *bulkConfigPushDataSource) Read(ctx context.Context, req datasource.ReadRequest, resp *datasource.ReadResponse) {
	var state bulkConfigPushDsModel
	resp.Diagnostics.Append(req.Config.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Basic logging.
	tflog.Info(ctx, "performing data source read", map[string]any{
		"data_source_name":            "prismasdwan_bulk_config_push",
		"terraform_provider_function": "Read",
		"site_template_id":            state.SiteTemplateId.ValueString(),
		"deployment_id":               state.DeploymentId.ValueString(),
	})

	// Prepare to run the command.
	svc := xNwmFxK.NewClient(d.client)

	// Prepare input for the API endpoint.
	input := xNwmFxK.ReadInput{}

	input.SiteTemplateId = StringValue(state.SiteTemplateId)

	input.DeploymentId = StringValue(state.DeploymentId)

	// Perform the operation.
	ans, err := svc.Read(ctx, input)
	if err != nil {
		resp.Diagnostics.AddError("Error reading config", err.Error())
		return
	}

	// Create the Terraform ID.
	var idBuilder strings.Builder
	idBuilder.WriteString(input.SiteTemplateId)

	idBuilder.WriteString(IdSeparator)
	idBuilder.WriteString(input.DeploymentId)

	// Store the answer to state.

	state.Tfid = types.StringValue(idBuilder.String())

	state.Etag = types.Int64PointerValue(ans.Etag)

	state.Id = types.StringPointerValue(ans.Id)

	var0, var1 := types.ListValueFrom(ctx, types.BoolType, ans.VariableMap)
	state.VariableMap = var0
	resp.Diagnostics.Append(var1.Errors()...)

	// Done.
	resp.Diagnostics.Append(resp.State.Set(ctx, &state)...)
}
