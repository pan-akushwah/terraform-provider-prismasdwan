# Probe Profile Configuration
probe_profile_name = "Production-Network-Probe-Profile"
description = "Production probe profile for comprehensive network monitoring, performance analysis, and connectivity diagnostics"
tags = ["terraform", "production", "probe", "monitoring", "network-diagnostics", "performance"]

# Probe Configuration IDs (1-8 max, no duplicates)
probe_config_ids = [
  "CloudFlare DNS ICMP Response"
]