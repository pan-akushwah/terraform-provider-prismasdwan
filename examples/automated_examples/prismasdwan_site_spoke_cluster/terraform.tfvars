# Spoke Cluster Configuration
spoke_cluster_name = "ProductionCluster"
site_name = "London"
description = "Production branch spoke cluster providing high availability and automatic failover for critical branch office connectivity"
tags = ["terraform", "production", "spoke-cluster", "branch", "failover", "ha"]

# Cluster Properties
advertisement_interval = 3.5
preempt = true